# Structural Health Report — ionrift-respite — 2026-06-04

## Baseline

- Commit: `3eaca5c` (chore: release v3.0.2)
- Tag: `pre-refactor/ionrift-respite/2026-06-04`
- Files: 103 (excluding `_archived`)
- Total lines: 56,544
- Total exports: 428

---

## Summary Dashboard

| Metric | Value |
|---|---|
| Files scanned | 103 |
| Total lines | 56,544 |
| God class candidates | 9 (7 🔴, 2 🟠) |
| Split-brain violations | 0 confirmed |
| DRY violations | 3 |
| Dead code items | 4 |
| Import tangles | 2 (both fixed) |
| Reconciliation opportunities | 2 |
| **Overall health** | 🔴 URGENT |

The `RestSetupApp.js` at **15,355 lines** with **~2,200 methods** is an extreme god class and the dominant structural liability. It alone accounts for 27% of total module code.

---

## File Size Distribution

| File | Lines | Signals |
|---|---|---|
| `apps/RestSetupApp.js` | 15,355 | 🔴 God class (4 concerns), 90 methods > 100 lines |
| `apps/StationActivityDialog.js` | 2,019 | 🔴 Mixed UI + Data + Orchestration |
| `services/StationInteractionLayer.js` | 1,760 | 🔴 Mixed Data + Orchestration + UI |
| `apps/ShortRestApp.js` | 1,707 | 🔴 Mixed Data + Generation + Orchestration + UI |
| `services/MealPhaseHandler.js` | 1,622 | 🟠 Mixed Data + Generation |
| `module.js` | 1,521 | 🟠 Mixed Data + Orchestration |
| `services/CompoundCampPlacer.js` | 1,410 | 🟡 Size threshold only |
| `apps/CampfireEmbed.js` | 1,127 | 🔴 Mixed Data + Orchestration + UI |
| `services/ResourceSink.js` | 1,114 | 🟠 Size threshold |
| `apps/delegates/MealDelegate.js` | 970 | 🟡 Size threshold |
| `apps/delegates/TravelResolutionDelegate.js` | 957 | 🔴 Mixed Data + Generation + Orchestration |
| `services/ActivityResolver.js` | 920 | 🟠 Mixed Data + Generation |
| `services/CampfirePhysics.js` | 860 | 🔴 Mixed Data + Orchestration + UI |
| `services/SettingsRegistrar.js` | 801 | 🟡 Size threshold |
| `services/SocketRouter.js` | 766 | 🟡 Size threshold |
| `services/SocketController.js` | 766 | 🟡 Size threshold, hub file (69 exports) |
| `apps/PackRegistryApp.js` | 750 | 🔴 Mixed Data + Orchestration + UI |
| `apps/delegates/WorkbenchDelegate.js` | 729 | 🟠 Mixed Data + Orchestration |
| `apps/RestConstants.js` | 704 | 🟡 Size threshold |
| `apps/delegates/EventsPhaseDelegate.js` | 686 | 🔴 Mixed Data + Generation + Orchestration |
| `services/TravelResolver.js` | 677 | 🟠 Mixed Data + Generation |

---

## Findings

### God Classes

#### RestSetupApp.js — 🔴 CRITICAL

**File:** `scripts/apps/RestSetupApp.js` (15,355 lines, ~2,200 method-like constructs)
**Signal:** God Class — mixed UI + Data + Generation + Orchestration
**Severity:** 🔴 CRITICAL

The single most problematic file in the module. It contains:
- **UI rendering**: `_prepareContext` (2,241 lines!), `_onRenderBindings` (649 lines), `render`
- **Data management**: `_saveRestState`, `_loadRestState`, `_loadData`, `_loadContentPacks`
- **Generation/Roll logic**: `receiveCampRollResult` (435 lines), `receiveDehydrationResult` (625 lines), `receiveTreeRollResult` (1,079 lines)
- **Orchestration**: `emitSubmissionUpdate`, `registerActiveRestApp`, `emitRestStarted`, `emitPhaseChanged`

**Key long methods:**
| Method | Lines |
|---|---|
| `_prepareContext()` | 2,241 |
| `receiveTreeRollResult()` | 1,079 |
| `_onRenderBindings()` | 649 |
| `receiveDehydrationResult()` | 625 |
| `_autoGrantPartyDiscoveries()` | 548 |
| `receiveCampRollResult()` | 435 |
| `setColdCampDuringActivity()` | 339 |
| `receiveRestSnapshot()` | 307 |
| `_refreshCampPitNoticeLayer()` | 298 |
| `receiveArmorToggle()` | 293 |

**Recommended decomposition** (future work, XL effort):

| Extract to | Responsibility | Est. lines |
|---|---|---|
| `delegates/RecoveryDelegate.js` | Recovery/resolution phase logic | ~1,500 |
| `delegates/EventTreeDelegate.js` | Decision tree roll handling | ~1,500 |
| `delegates/CampSetupDelegate.js` | Camp placement UI, cold camp, fire level | ~1,200 |
| `delegates/ActivityPhaseDelegate.js` | Activity assignment, auto-grant | ~800 |
| `RestSetupContext.js` | `_prepareContext` extraction | ~2,200 |

This decomposition would require careful attention to shared state (`this._phase`, `this._engine`, `this._characterChoices` etc.) which is accessed across all concerns.

#### ShortRestApp.js — 🔴 CRITICAL

**File:** `scripts/apps/ShortRestApp.js` (1,707 lines, ~252 methods)
**Signal:** God Class — mixed UI + Data + Generation + Orchestration

Similar pattern to RestSetupApp at a smaller scale. Mixes:
- Roll serialization/deserialization
- Workbench state networking
- HD rolling and recovery logic
- UI rendering

#### StationActivityDialog.js — 🔴 CRITICAL

**File:** `scripts/apps/StationActivityDialog.js` (2,019 lines, ~273 methods)
**Signal:** Mixed UI + Data + Orchestration

Handles feast credit logic, station tab building, craft result auto-commit (414 lines), and socket emission in a single class.

---

### Split-Brain Violations

No confirmed split-brain violations detected. The codebase uses FoundryVTT's ApplicationV2 pattern with `_prepareContext` → Handlebars rendering consistently. Event handlers generally call `this.render()` rather than reconstructing DOM manually.

---

### DRY Violations

#### DRY-001: itemIsWorkbenchUnidentified divergence — 🟠 REFACTOR

**Files:** `apps/RestSetupApp.js` line 156 vs `apps/delegates/WorkbenchDelegate.js` line 40

The RestSetupApp version was dead code (defined but never called). WorkbenchDelegate has a more restrictive version with an additional `!quartermasterLatent.identified` guard. The RSA copy was removed in REFACTOR-003.

**Status:** Dead copy removed. Divergence flagged for reconciliation (see below).

#### DRY-002: resolveItemFromDropEvent / itemIsDnD5ePotionType — ✅ FIXED

Identical implementations in RestSetupApp.js and WorkbenchDelegate.js extracted to `util/itemDropUtils.js` in REFACTOR-003.

#### DRY-003: SAFE_REST_SPOT_EXCLUDED_ACTIVITY_IDS — ✅ FIXED

Identical constant defined in both `apps/RestConstants.js` and `services/ActivityResolver.js`. The RestConstants copy was exported but never imported. Removed in REFACTOR-004.

---

### Dead Code

| Item | Location | Status |
|---|---|---|
| `_archived/CampfireApp.js` (873 lines) | `scripts/_archived/` | Not imported anywhere. Referenced in comments only. Retained as archived reference. |
| `itemIsWorkbenchUnidentified` in RSA | `apps/RestSetupApp.js:172` | Defined but never called. **Removed in REFACTOR-003.** |
| `itemIsNativeUnidentified` in RSA | `apps/RestSetupApp.js:163` | Defined but never called. **Removed in REFACTOR-003.** |
| 13 unused socket emitters | `services/SocketController.js` | `emitCampLightFireRequest`, `emitMealChoice`, `emitMealDayConsumeRequest`, `emitMealDayConsumed`, `emitDehydrationSaveRequest`, `emitDehydrationSaveResult`, `emitDehydrationResultsBroadcast`, `emitCopySpellApproved`, `emitCopySpellDeclined`, `emitCopySpellRollPrompt`, `emitCopySpellResult`, `emitCopySpellBusy`, `emitShortRestDismissed`. May be reserved for cross-module consumers. Flagged, not removed. |

---

### Import Tangles

#### TANGLE-001: Circular — CompoundCampPlacer ↔ RestConstants — ✅ FIXED

**Cycle:** `CompoundCampPlacer.js` imported `PLACEHOLDER_CAMP_STATION` from `RestConstants.js`, which imported `isGearDeployed` from `CompoundCampPlacer.js`.

**Fix:** Moved `PLACEHOLDER_CAMP_STATION` into `CompoundCampPlacer.js` where it is exclusively consumed. The dependency is now unidirectional (RestConstants → CompoundCampPlacer).

#### TANGLE-002: Circular — StationActivityDialog → module.js → RestSetupApp → CampfireMakeCampDialog → StationActivityDialog — ✅ FIXED

**Cycle:** `StationActivityDialog` and `SocketRouterHandlers` imported `_refreshGmRestIndicator` from `module.js`, which imports `RestSetupApp.js`, which imports `CampfireMakeCampDialog.js`, which imports `StationActivityDialog.js`.

**Fix:** Both consumers now import `refreshGmRestIndicator` directly from `services/RejoinManager.js`, eliminating the transitive cycle.

#### TANGLE-003: Structural — module.js ↔ RestSetupApp.js (bidirectional) — 🟡 REVIEW

`module.js` imports `RestSetupApp` (line 9) and `RestSetupApp` imports from `module.js` (lines 100-114). This is inherent to the entry-point/app-registration pattern and works at runtime via ESM live bindings. Not a functional issue, but increases cognitive load and coupling.

**Recommendation:** Extract state management functions (`registerActiveRestApp`, `clearActiveRestApp`, `setActiveRestData`, etc.) into a dedicated `services/RestAppRegistry.js` in a future session.

#### Hub Files

| File | Imported by |
|---|---|
| `lib/Logger.js` | 33 files |
| `services/partyActors.js` | 16 files |
| `services/ItemClassifier.js` | 9 files |
| `services/TerrainRegistry.js` | 8 files |
| `services/GrantLedger.js` | 8 files |

Logger being a hub is expected. `partyActors.js` at 23 lines serving 16 consumers is well-factored.

---

### Reconciliation Opportunities

#### RECONCILE-001: itemIsWorkbenchUnidentified implementation drift

**RestSetupApp version (removed):**
```js
const isQmMasked = !!quartermasterLatent && quartermasterLatent.kind !== "mundane";
```

**WorkbenchDelegate version (canonical):**
```js
const isQmMasked = !!quartermasterLatent
    && !quartermasterLatent.identified
    && quartermasterLatent.kind !== "mundane";
```

The WorkbenchDelegate adds `!quartermasterLatent.identified` — preventing already-identified cursed items from appearing as workbench candidates. The RSA version was less restrictive but also dead code. No reconciliation action needed since the dead copy was removed.

#### RECONCILE-002: Indicator wrapper pattern in module.js

Functions like `_showGmRestIndicator`, `_removeGmRestIndicator`, `_refreshGmRestIndicator` in module.js are thin wrappers around `RejoinManager.js` exports. RestSetupApp and ShortRestApp import these wrappers rather than the direct functions. This adds an unnecessary indirection layer.

**Recommendation:** In a future session, have RestSetupApp/ShortRestApp import directly from `services/RejoinManager.js` (same pattern applied to StationActivityDialog/SocketRouterHandlers in REFACTOR-002). The wrappers can then be removed from module.js.

---

## Refactor Queue (Prioritized)

| Priority | ID | Title | Severity | Effort | Risk | Status |
|---|---|---|---|---|---|---|
| 1 | REFACTOR-001 | Break circular import CompoundCampPlacer ↔ RestConstants | 🔴 | S | None | ✅ Done |
| 2 | REFACTOR-002 | Break circular import StationActivityDialog → module.js | 🔴 | S | None | ✅ Done |
| 3 | REFACTOR-003 | Extract duplicated item utilities to shared module | 🟠 | S | None | ✅ Done |
| 4 | REFACTOR-004 | Remove dead SAFE_REST_SPOT_EXCLUDED_ACTIVITY_IDS from RestConstants | 🟡 | S | None | ✅ Done |
| 5 | — | Decompose RestSetupApp._prepareContext (2,241 lines) | 🔴 | XL | Medium | Queued |
| 6 | — | Extract receiveTreeRollResult (1,079 lines) to EventTreeDelegate | 🔴 | L | Medium | Queued |
| 7 | — | Extract receiveDehydrationResult (625 lines) to RecoveryDelegate | 🔴 | L | Medium | Queued |
| 8 | — | Extract receiveCampRollResult (435 lines) to CampSetupDelegate | 🟠 | M | Low | Queued |
| 9 | — | Move indicator wrappers out of module.js (RECONCILE-002) | 🟡 | S | Low | Queued |
| 10 | — | Extract RestAppRegistry from module.js (TANGLE-003) | 🟡 | M | Low | Queued |
| 11 | — | Remove 13 unused socket emitters from SocketController | 🟡 | S | Medium* | Queued |

*Medium risk on #11 because unused emitters may be consumed by external modules.

---

## Post-Refactor Verification

| Metric | Before | After | Delta |
|---|---|---|---|
| Total files | 103 | 104 | +1 |
| Total lines | 56,544 | 56,482 | −62 |
| Total exports | 428 | 429 | +1 |
| Broken imports | 0 | 0 | — |
| Circular imports | 2 | 0 | −2 |

**New file:** `scripts/util/itemDropUtils.js` (59 lines) — shared item-drop classification utilities.

**Net line reduction** comes from removing dead code and duplicate function bodies. The +1 export is the net of removing `PLACEHOLDER_CAMP_STATION` and `SAFE_REST_SPOT_EXCLUDED_ACTIVITY_IDS` from RestConstants (−2) while adding 3 exports in the new shared utility file (+3).

---

## Executed Refactors

### REFACTOR-001: Break circular import CompoundCampPlacer ↔ RestConstants

**Commit:** `f9dc3e5`
**Change:** Moved `PLACEHOLDER_CAMP_STATION` constant into `CompoundCampPlacer.js` where it is exclusively consumed. Removed the definition from `RestConstants.js`. This eliminates the bidirectional dependency.
**Verification:** All imports resolve. No export regression (constant was only consumed internally).

### REFACTOR-002: Break circular import StationActivityDialog → module.js

**Commit:** `a78dc16`
**Change:** `StationActivityDialog.js` and `SocketRouterHandlers.js` now import `refreshGmRestIndicator` directly from `services/RejoinManager.js` instead of the re-exported `_refreshGmRestIndicator` wrapper in `module.js`.
**Verification:** Circular import detection confirms 0 cycles remaining. All imports resolve.

### REFACTOR-003: Extract duplicated item utilities to shared module

**Commit:** `810d3f4`
**Change:** Created `scripts/util/itemDropUtils.js` with `itemIsNativeUnidentified`, `itemIsDnD5ePotionType`, and `resolveItemFromDropEvent`. Updated `WorkbenchDelegate.js` to import from the shared file. Removed dead copies from `RestSetupApp.js` (including the never-called `itemIsWorkbenchUnidentified`).
**Verification:** All imports resolve. No public API regression.

### REFACTOR-004: Remove dead SAFE_REST_SPOT_EXCLUDED_ACTIVITY_IDS from RestConstants

**Commit:** `688c9f8`
**Change:** Removed the exported-but-never-imported constant from `RestConstants.js`. The canonical copy remains in `services/ActivityResolver.js`.
**Verification:** No consumers affected (grep confirms zero importers).

---

## Failed Refactors

None. All four executed refactors passed structural verification.

---

## Delta from Previous Run

*No previous report found for this module.*

---

## Observations

1. **RestSetupApp.js dominance:** At 15,288 lines post-refactor, this file remains the critical structural liability. It has already been partially decomposed (delegates exist for Crafting, Meal, CopySpell, DetectMagic, Events, Travel, Sound, Workbench, CampCeremony), but the core app still handles Setup, Activity assignment, Camp management, Recovery, and all socket receive handlers directly. The existing delegate pattern is the right direction.

2. **Console.warn usage:** 203 instances of `console.warn`/`console.log` across the codebase. Most are wrapped in try/catch error handlers (appropriate) or debug guards. No widespread Logger bypass pattern detected — the `Logger` wrapper from `lib/Logger.js` is used for normal operational logging.

3. **No TODO/FIXME accumulation:** Zero TODO or FIXME comments found in the scanned files.

4. **No commented-out code:** No multi-line commented-out executable code blocks detected.

5. **Module boundary discipline:** Only one cross-module import exists (`services/TerrainRegistry.js` → `ionrift-library`), which is declared in `module.json` relationships. No undeclared cross-module coupling.
